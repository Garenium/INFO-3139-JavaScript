import React from "react";
import {
  AppBar,
  Toolbar,
  Typography,
} from "@mui/material";

const Bar = () => {
  return(
  <AppBar>
    <Toolbar>
      <Typography variant="h6" color="inherit">
        INFO3139 - Socket IO
      </Typography>
    </Toolbar>
  </AppBar>
  );
};

export default Bar;
